import { StyleSheet } from "react-native";

export const button = StyleSheet.create({
    default: {
        backgroundColor: '#A370F7',
        padding: 15,
        borderRadius: 7,
        alignItems: 'center'
    },

    defaultText: {
        color: '#FFF',
        fontWeight: 600
    }
})