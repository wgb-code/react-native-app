import { StyleSheet } from "react-native";

export const text = StyleSheet.create({
    title: {
        color: '#FFF',
        fontSize: 24,
        fontWeight: 'bold'
    },

    titleMargin: {
        marginTop: 50,
        marginBottom: 20,
    },

    badge: {
        color: '#FFF',
        fontSize: 18,
        fontWeight: '600'
    }
})